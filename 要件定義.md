# Splatoonブキガチャ 要件定義

## 1. 概要

Splatoon 3のブキをランダムに抽選するPC・スマートフォン向けWebアプリとする。HTML・CSS・JavaScriptだけで動作する静的サイトとしてGitHub Pagesへ公開し、専用バックエンド、外部API、外部CDNは使用しない。

## 2. 対応環境とUI

- PC・スマートフォンの現行ブラウザを対象とし、320px以上の表示幅へレスポンシブ対応する。
- デフォルトは明るくやさしいライトテーマとする。
- PCでは左側に画面高いっぱいの固定サイドバーを配置し、900px以下では開閉式のサイドバーとする。
- サイドバーにモード、プレイヤー数、ステータス、データ管理を配置する。
- メイン領域に今回のブキ、抽選ボタン、ブキリストを配置する。
- 抽選結果は2列表示、ブキリスト内のブキは4列表示を基本とする。
- UI操作はキーボードでも行え、ボタンと入力欄には適切なラベルを付ける。

## 3. 抽選モード

### 3.1 プライベートマッチ

- 2〜10人を指定して人数分のブキを抽選する。
- 1回の抽選内で同じブキが重複してもよい。
- 各結果カードにプレイヤー名入力欄を表示する。

### 3.2 オープンマッチ

- 2〜4人を指定して人数分のブキを抽選する。
- 1回の抽選内で同じブキが重複してもよい。
- 各結果カードにプレイヤー名入力欄を表示する。
- 抽選後、表示中のブキをまとめてオープンマッチの抽選対象から除外できる。

### 3.3 ブキ統一

- チーム全員で使うブキを1つ抽選する。
- プレイヤー名入力欄は表示しない。
- 抽選されたブキはブキ統一の抽選対象から自動で除外する。

### 3.4 共通ルール

- 抽選時は現在のモードで除外されていないブキだけを使用する。
- 除外状態は`private`、`open`、`unity`で独立して管理する。
- 抽選対象が0件の場合は抽選せず、解除方法を含むエラーを表示する。
- モード変更時はプレイヤー名と結果を初期化する。

## 4. プレイヤー名と結果

- 入力前は`Player 1`、`Player 2`のようなプレースホルダーを表示する。
- プレイヤー名は最大60文字とする。
- プレイヤー名はページ内の一時状態とし、JSONおよび`localStorage`へ保存しない。
- 長いブキ名はカード幅に収まるまで段階的に縮小する。
- ブキ名に対応する画像がある場合は結果カード右側へ表示する。
- 画像がない、または読み込みに失敗した場合も抽選結果の文字は表示する。

## 5. ブキリスト

- `weapon-list.json`の順番を維持してカテゴリごとに表示する。
- カテゴリ名の末尾が「系」の場合、画面上では「系」を省略する。
- 各カテゴリに「対象数 / 総数」を表示する。
- 初回表示では全カテゴリを閉じる。
- カテゴリ単位の開閉と、全カテゴリの一括開閉を提供する。
- ブキカードを押すと現在のモードの対象／除外を切り替える。
- 各カテゴリに「すべて含める」「すべて除外」を提供する。
- 現在のモード全体に対する「すべて除外」「除外をすべて解除」を提供する。
- 除外中のブキは背景・文字・画像を薄くし、ブキ名に取消線を付ける。
- カテゴリが閉じている間はブキ画像を取得しない。

## 6. ステータスと通知

- サイドバーへ現在のモードの抽選対象数、総数、除外数を表示する。
- 完了・エラー通知は画面を押し下げない固定表示とする。
- 一時的な完了通知は原則3秒で消す。

## 7. 設定

- ガチャアニメーションのON／OFFを切り替えられる。
- 初期値はONとする。
- 設定は`ink-draw-settings-v1`をキーとして`localStorage`へ保存する。
- 設定データがない、または破損している場合はONで起動する。

## 8. データ管理

### 8.1 JSON形式

```json
{
  "version": 1,
  "items": [
    {
      "id": "weapon-unique-id",
      "name": "スプラシューター",
      "category": "シューター",
      "excluded": {
        "private": false,
        "open": false,
        "unity": false
      }
    }
  ]
}
```

- ルートはオブジェクトとする。
- `version`は数値`1`のみ受け付ける。
- `items`は1件以上の配列とする。
- `id`、`name`、`category`は空でない文字列とし、前後の空白を除去する。
- `id`はデータ内で一意とする。
- `excluded.private`、`excluded.open`、`excluded.unity`はすべて真偽値とする。
- 旧キー`multi`、`four`、`single`は受け付けない。

### 8.2 インポート

- 5MB以下のJSONファイルを読み込める。
- 読み込み前に上記形式を検証する。
- 不正なデータは適用せず、理由をエラー表示する。
- 正常時はブキリストを置き換え、結果とプレイヤー名を初期化する。

### 8.3 エクスポート

- `version`、全項目、3モードの除外状態をJSONとして出力する。
- プレイヤー名、抽選結果、現在のモード、プレイヤー数は含めない。

### 8.4 初期化

- 3モードすべての除外だけを解除する操作を提供する。
- 確認ダイアログ後、`weapon-list.json`へ完全に戻す操作を提供する。

## 9. ブラウザ保存と復旧

- ブキリストと除外状態の保存キーは`ink-draw-state-v2`とする。
- ガチャアニメーション設定の保存キーは`ink-draw-settings-v1`とする。
- ブキリストと3モードの除外状態を変更のたびに`localStorage`へ保存する。
- 保存済みデータが正常な場合は`weapon-list.json`より優先する。
- 保存済みデータが不正・破損している場合は`weapon-list.json`から自動復旧し、復旧データを保存し直して通知する。
- `localStorage`を利用できない場合は初期データで起動し、保存できないことを通知する。

## 10. ブキ画像

- 画像は`_images/MainWeapons/`へ配置する。
- 表示名とファイル名の対応は`weapon-icons.js`で管理する。
- ファイル名は英小文字・数字・ハイフンによるケバブケースを原則とする。
- GitHub Pagesでの利用を考慮し、実ファイルと参照文字列の大文字・小文字を完全一致させる。
- ブキリスト画像は`loading="lazy"`と`decoding="async"`を用いる。
- 初期表示時に全画像を一括先読みしない。

## 11. セキュリティ

- JSONの構文、必須フィールド、データ型、ID重複を検証する。
- 読み込んだ文字列は`textContent`または入力値として表示し、HTMLとして解釈しない。
- JSONから外部スクリプトや任意コードを実行しない。
- アプリのフォント、スタイル、スクリプト、アイコンは自己配信し、第三者CDNへ接続しない。
- Content Security Policyを設定し、読み込み元を原則として同一オリジンへ制限する。

## 12. ファイル構成と公開

```text
.
├── index.html
├── style.css
├── script.js
├── weapon-list.json
├── weapon-icons.js
├── README.md
├── 要件定義.md
├── .gitignore
├── .nojekyll
└── _images/
    ├── MainWeapons/
    ├── Gold-Capsule.png
    ├── Shell-Out-Machine.png
    ├── top-icon.png
    └── yajirusi.png
```

- リポジトリの`main`ブランチのルートをGitHub Pagesで公開する。
- `_images`をそのまま配信するため`.nojekyll`を維持する。
- JavaScriptが無効、初期JSONが取得不能、または画像が欠落している場合でも、不正なコードを実行しない。
